from django.db import models

# The summary app primarily handles dashboard views and KPI calculations
# which are mostly done through views and services rather than models
# This file exists to make it a proper Django app